import StudentPlansLayout from "../plans/layout";

export default function ReportsLayout({ children }: { children: React.ReactNode }) {
  return <StudentPlansLayout>{children}</StudentPlansLayout>;
}
