import { kindLabel, PlanItem, timeText } from "@/lib/planning";

export default function PlanBlock({ item, actions, plannedLabel = false }: { item: PlanItem; actions?: React.ReactNode; plannedLabel?: boolean }) {
  const detail = [item.chapter_name, item.topic_name].filter(Boolean).join(" ← ");
  return <article className={`planning-block planning-block-${item.kind.toLowerCase()}`}><div className="planning-block-top"><span className="planning-badge">{kindLabel[item.kind]}</span>{item.start_time && item.end_time && <span dir="ltr">{timeText(item.start_time)}–{timeText(item.end_time)}</span>}</div><strong>{item.title || item.subject_name || kindLabel[item.kind]}</strong>{item.title && item.subject_name && <span>{item.subject_name}</span>}{detail && <span className="planning-block-detail">{detail}</span>}{plannedLabel && <strong className="planning-section-label">برنامه مشاور</strong>}<div className="planning-block-meta">{item.planned_duration_minutes != null && <span>{item.planned_duration_minutes} دقیقه</span>}{item.test_count != null && <span>{item.test_count} تست</span>}</div>{item.note && <p className="planning-note">{item.note}</p>}{actions && <div className="planning-block-actions">{actions}</div>}</article>;
}
